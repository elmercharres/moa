# backend.qa.s3.hcl — Backend API stack, ambiente QA
# Copiar a backend.qa.s3.hcl (sin .example) y confirmar valores con MOA.
# Este archivo NO debe estar versionado en Git (.gitignore lo excluye).
#
# Key confirmada por Infraestructura MOA — auditoría 2026-07-06, §14.
bucket         = "moaplatformiac-data-analytics-qa-tfstate"
key            = "portal-creditos-gestion-crediticia-backend-qa/terraform.tfstate"
region         = "us-east-1"
use_lockfile   = true
encrypt        = true
